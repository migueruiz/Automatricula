{
    "name": "automatricula",
    "version": "0.0.1",
    "dependencies": {
        "typescript": "^5.2.2"
    },
    "devDependencies": {
        "jest": "^0.34.6",
        "@types/jest": "^29.5.8",
        "ts-jest": "^29.1.1"
    },
    "scripts": {
        "check": "tsc --noEmit src/estudiante.ts src/asignatura.ts",
        "test": "jest",
        "build": "tsc",
        "start": "node dist/estudiante.js"
    }
}
