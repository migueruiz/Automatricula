{
    "name": "automatricula",
    "version": "0.0.1",
    "dependencies": {
        "typescript": "^5.2.2"
    },
    "devDependencies": {
        "vitest": "^0.34.6"
    },
    "scripts": {
        "check": "tsc --noEmit src/estudiante.ts src/asignatura.ts",
        "test": "vitest --no-watch",
        "build": "tsc",
        "start": "node dist/estudiante.js"
    }
}
